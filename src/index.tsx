import * as React from 'react';
import * as reactDom from 'react-dom';
import App from './components/App/App';

reactDom.render(<App />, document.getElementById('index')); // eslint-disable-line no-console
